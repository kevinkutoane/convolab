# ADR 0017: Structured Problem Details

**Status:** Accepted

The API uses RFC 7807 responses with stable platform error codes and correlation IDs. Internal stack traces and provider payloads are not returned to clients.
